using RenamerMediaFiles.Models;

namespace RenamerMediaFiles.Tests;

public class FileItemModelTests
{
    // private string GetAdditionalName(IEnumerable<StringModel> removingNameParts)
    // public void RefreshFileInfo(FileInfo fileInfo, string rootPath)
    // internal void ReadAllMetadata(string newNameFormat, string? additionalName)
    // internal void SelectSingleMetadata()
    //
    // [Fact]
    // public void ApplyExtension_ContainAttributeTag_ReturnsExpectedDateTime()
    // {
    //     new FileItemModel()
    //     var sourceDateTime = DateTime.MinValue;
    //     var offsetHour = 1.5f;
    //     var expectedDate = sourceDateTime.AddHours(offsetHour);
    //     var metadataStub = new FileMetadataDirectory();
    //     metadataStub.Set(FileMetadataDirectory.TagFileName, "testfile");
    //     var extensionStub = new Mock<MetaDateTimeExtension>(metadataStub.Name, MetaTypes.AttributeTag, metadataStub.Name, "File Name", offsetHour).Object;
    //     
    //     var result = MediaMetadataWrapper.ApplyMetadataExtensions(new[] { metadataStub }, new[] { extensionStub }, sourceDateTime);
    //     
    //     Assert.Equal(expectedDate, result);
    // }
    
}